# Agent Rules

**ANTI-DELETION RULE**
No file under `diagnostics/`, `probes/`, or any git-tracked path may be deleted, moved, or "cleaned up" as part of a general cleanup/reorg step. Deletion of a tracked file requires an explicit, per-file request in the current task's scope.
